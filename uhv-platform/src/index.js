import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';  // Make sure this exists with Tailwind directives
import UHVPlatform from './App.js';  // Import your component

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  
    <UHVPlatform />  
  
);
